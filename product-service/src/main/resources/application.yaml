spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/admin
  application:
    name: product-service

eureka:
  client:
    service-url:
      defaultZone:
        http://localhost:8761/eureka
